micronaut:
  application:
    name: micronaut-crud
datasources:
  default:
    url: jdbc:mysql://localhost:3306/mn_playground
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: none
        show_sql: true
flyway:
  datasources:
    default:
      enabled: true